# Example environment variables for the SvelteKit application

# Dynamic private environment variables (server-side only)
DYNAMIC_PRIVATE_KEY=your-private-key-here

# Dynamic public environment variables (exposed to client)
PUBLIC_DYNAMIC_KEY=your-public-key-here

# Static private environment variables (server-side only, bundled at build time)
STATIC_PRIVATE_KEY=your-static-private-key-here

# Static public environment variables (exposed to client, bundled at build time)
PUBLIC_STATIC_KEY=your-static-public-key-here
